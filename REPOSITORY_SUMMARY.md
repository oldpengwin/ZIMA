# ZIMA — Repository Status

**Superseded.** This file's content (the backend-rebuild pass writeup) has
been folded into `README.internal.md`, which is now the single source of
truth for "what's actually real, what's broken, what's still needed"
across every engineering pass — including this file's original rewrite,
the storage/caching/Discord-hooking pass, and the production-readiness
audit. Kept here as a short pointer, not deleted, so nothing that already
links to this filename 404s.

Read `README.internal.md`. Do not include either file in a public release
— see that file's top section for what that means and why.
