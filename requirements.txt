# ZIMA Platform - Python Dependencies
#
# Security pass (2026-08-12): removed dependencies that were declared but never
# imported anywhere in src/ (they accounted for the large majority of the
# Dependabot advisories), and bumped the ones that ARE used to patched versions.
# Verify with `pip install -r requirements.txt` + `pytest tests/backend/`.

# FastAPI Core
fastapi==0.109.2
uvicorn==0.27.1
python-dotenv==1.2.2          # was 1.0.0 (CVE-2026-28684); de-duplicated (was listed twice)

# Database
psycopg2-binary==2.9.10        # was 2.9.9 (adds cp313 wheels; no source build needed)
SQLAlchemy==2.0.29
pgvector==0.2.5
alembic==1.13.1

# Authentication
python-jose==3.4.0             # was 3.3.0 (CVE-2024-33663 / CVE-2024-33664, JWT alg confusion). USED: core/auth.py, api/routes.py
# REMOVED as unused (not imported anywhere in src/) — these were leftovers from
# the old mock-password auth that real Discord OAuth2 replaced:
#   authlib   (was 1.3.0 — 11 CVEs incl. CVE-2024-37568 JWS confusion)
#   passlib   (was 1.7.4)
#   bcrypt    (was 4.1.2)

# API / forms
email-validator==2.1.0
python-multipart==0.0.31       # was 0.0.6 (multipart DoS chain, CVE-2024-24762 .. CVE-2026-53540). FastAPI form-parse dep; ships the `multipart` import shim so it stays compatible

# Redis + rate limiting
redis==5.0.1
slowapi==0.1.8                 # wired in src/main.py

# WSGI server (production)
gunicorn==23.0.0               # was 21.2.0 (CVE-2024-1135 request smuggling, CVE-2024-6827)

# Testing / seed data
# httpx is also a RUNTIME dep now (src/core/auth.py uses it for the real Discord
# OAuth2 token exchange), not just the test client.
pytest==8.0.0                  # NOTE: CVE-2025-71176 (dev-only, low) is fixed in 9.0.3 — a pytest major bump; left for a tested upgrade
httpx==0.26.0
pytest-asyncio==0.23.5
Faker==24.4.0

# Discord Bot (separate service)
discord.py==2.3.2

# Declared but currently unused (kept — not vulnerable, and likely wanted soon).
# Re-pin to a current version when the feature that needs it is actually built:
#   google-generativeai (Gemini)     — GEMINI_API_KEY exists but no code uses it yet
#   apscheduler (background jobs)     — no scheduler wired yet
#   loguru (logging)                  — code uses stdlib logging
# REMOVED as unused: sentence-transformers / transformers / torch — the embedding
# feature isn't implemented (nothing imports them; ~65 advisories eliminated).
# Re-add a CURRENT pinned version of these when embeddings are actually built.
google-generativeai==0.5.2
apscheduler==3.10.4
loguru==0.7.2
